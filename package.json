{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "android:init": "npx cap add android",
    "android:build": "npx cap copy android && npx cap open android",
    "ios:init": "npx cap add ios",
    "ios:build": "npx cap copy ios && npx cap open ios",
    "desktop:init": "npm install @capacitor/electron && npx cap add electron",
    "desktop:build": "npx cap copy electron && npx cap open electron"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^22.13.11",
    "express": "^5.1.0",
    "jszip": "^3.10.1",
    "mammoth": "^1.9.1",
    "pdf-dist": "^1.0.0",
    "xlsx": "^0.18.5"
  }
}
